<div class="jumbotron">
  <h1 class="display-4">Bem-vindo ao Blog Nodejs</h1>
  <p class="lead">Este é um BLOG simples, desevoldido com Nodejs + express + MongoDB</p>
  <hr class="my-4">
  <p>Aqui você vai poder ler sobre as novidades do mundo da computação</p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
  </p>
</div>
<hr>

<h2>Postagens recentes:</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">

        <h3>{{titulo}}</h3>
        <p>{{descrição}}</p>

        <a  href="/postagem/{{slug}}"<button class="btn btn-success">Leia mas</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
        <small>Data de Publicação: {{data}}</small>
        

        </div>
    </div>
    {{else}}
        <h5>Nenhuma Postagem</h5>
    {{/each}}